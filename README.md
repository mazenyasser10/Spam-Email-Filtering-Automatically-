# Spam-Email-Filtering-Automatically-
•Developed a GUI-based spam email detection system using machine learning. The system was trained on email data to automatically classify messages as spam or not spam. Users can enter an email text into the interface, and the system predicts the result, focusing on data preprocessing, model training, classification, and GUI integration.
